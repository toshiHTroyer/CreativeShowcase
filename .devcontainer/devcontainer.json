{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "postCreateCommand": "cd creativeshowcase && npm install",
  "postStartCommand": "cd creativeshowcase && npm run dev",
  "remoteEnv": {
    "PORT": "${PORT}",
    "DSN": "${DSN}",
    "SESSION_SECRET": "${SESSION_SECRET}"
  }
}